<h3>意味ネットワーク(仮)</h3>
<div id="semnet"></div>
<script type="text/javascript">
// create an array with nodes
var nodes = [];
for (var i = 0; i < (gon.entnum); i++) {
  nodes.push({id: ['ent',gon.ent[i]].join("_"), label: (gon.ent[i]), color: gon.color});
}
for (var i = 0; i < (gon.valnum); i++) {
  nodes.push({id: ['val',gon.val[i]].join("_"), label: (gon.val[i]), color: gon.color});
}
// create an array with edges
var edges = [];
for (var ent in gon.triad) {
  for ( var val in gon.triad[ent] ) {
    edges.push({from: ['ent',ent].join("_"), to: ['val',val].join("_"), label: gon.triad[ent][val], arrows: 'to'});
  }
}

// create a network
var container = document.getElementById('semnet');
var data = {
  nodes: nodes,
  edges: edges
};
var options = {
nodes: { shape: 'box', font: { size: 25} },
edges: { arrow: 'to', font: { size: 16, align: 'top' } }
};
var network = new vis.Network(container, data, options);
</script>
